# dsh-control-plane values
replicaCount: 1

image:
  dshWeb: runzhliu/deepseek-harness:0.1.0-rc.6
  pullPolicy: IfNotPresent

# dsh web listen (public; in-process OIDC gate via dsh-web-auth fork)
dshWeb:
  port: 3080
  # public authority(ies) the /api trust fence accepts (--trusted-host)
  trustedHosts: []

auth:
  oidcIssuer: ""
  oidcClientId: ""
  redirectUri: ""
  adminGroups: "dsh-admins"
  # secret refs
  oidcClientSecretRef: ""
  sessionSecretRef: ""

ingress:
  enabled: false
  className: ""
  host: ""
  tlsSecret: ""

resources: {}
nodeSelector: {}
tolerations: []
affinity: {}

# Istio exposure (preferred: *.svc.visecy.top via internal-gateway)
istio:
  enabled: false
  gateway: istio-system/internal-gateway
  host: ""
