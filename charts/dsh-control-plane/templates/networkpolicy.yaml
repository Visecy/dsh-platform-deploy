apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "dsh-control-plane.fullname" . }}
  labels:
    {{- include "dsh-control-plane.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ include "dsh-control-plane.name" . }}
  policyTypes: [Ingress]
  ingress:
    # the gate (3080) is reachable from the cluster (ingress/port-forward);
    # dsh-web (3000) stays loopback-only inside the pod.
    - ports:
        - protocol: TCP
          port: 3080
